Django==5.2.12
whitenoise==6.12.0
gunicorn==21.2.0
django-environ==0.11.2

# For SQL Server (if you need it)
pyodbc==5.3.0
# Use this instead of django-pyodbc-azure for Django 5.2:
django-mssql-backend==2.8.1

# Optional - remove these if you don't need them:
# argon2-cffi==21.3.0
# bcrypt==4.0.1
# Pillow==8.4.0
# psycopg2-binary==2.9.8  # Only if using PostgreSQL